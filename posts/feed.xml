<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en">
	<title>My Blog</title>
	<subtitle>My blog site.</subtitle>
	<link rel="self" type="application/atom+xml" href="https://josephbak.github.io/posts/feed.xml"/>
  <link rel="alternate" type="text/html" href="https://josephbak.github.io/posts/"/>
  
	<updated>2026-08-24T00:00:00+00:00</updated>
	
	<id>https://josephbak.github.io/posts/feed.xml</id>
	<entry xml:lang="en">
		<title>Lowering MX Block-Matmul to linalg.generic with Block-Scale Affine Maps</title>
		<published>2026-08-24T00:00:00+00:00</published>
		<updated>2026-08-24T00:00:00+00:00</updated>
		<link rel="alternate" type="text/html" href="https://josephbak.github.io/posts/lowering-mx-block-matmul/"/>
		<id>https://josephbak.github.io/posts/lowering-mx-block-matmul/</id>
    
		<content type="html" xml:base="https://josephbak.github.io/posts/lowering-mx-block-matmul/">&lt;!-- KaTeX includes (inline; requires markdown.render_unsafe = true) --&gt;
&lt;link rel=&quot;stylesheet&quot; href=&quot;https:&#x2F;&#x2F;cdn.jsdelivr.net&#x2F;npm&#x2F;katex@0.16.10&#x2F;dist&#x2F;katex.min.css&quot;&gt;
&lt;script defer src=&quot;https:&#x2F;&#x2F;cdn.jsdelivr.net&#x2F;npm&#x2F;katex@0.16.10&#x2F;dist&#x2F;katex.min.js&quot;&gt;&lt;&#x2F;script&gt;
&lt;script defer src=&quot;https:&#x2F;&#x2F;cdn.jsdelivr.net&#x2F;npm&#x2F;katex@0.16.10&#x2F;dist&#x2F;contrib&#x2F;auto-render.min.js&quot;&gt;&lt;&#x2F;script&gt;
&lt;script&gt;
 document.addEventListener(&quot;DOMContentLoaded&quot;, function() {
 renderMathInElement(document.body, {
 delimiters: [
 {left: &quot;$$&quot;, right: &quot;$$&quot;, display: true},
 {left: &quot;\\[&quot;, right: &quot;\\]&quot;, display: true},
 {left: &quot;$&quot;, right: &quot;$&quot;, display: false},
 {left: &quot;\\(&quot;, right: &quot;\\)&quot;, display: false}
 ],
 throwOnError: false
 });
 });
&lt;&#x2F;script&gt;
&lt;h2 id=&quot;why-the-matmul-can-t-be-a-matmul&quot;&gt;Why the matmul can&#x27;t be a matmul&lt;a class=&quot;zola-anchor&quot; href=&quot;#why-the-matmul-can-t-be-a-matmul&quot; aria-label=&quot;Anchor link for: why-the-matmul-can-t-be-a-matmul&quot; style=&quot;visibility: hidden;&quot;&gt;&lt;&#x2F;a&gt;
&lt;&#x2F;h2&gt;
&lt;p&gt;A matrix multiply contracts two operands along a shared dimension. Both are uniform: every element is one value of one type, addressed the same way. &lt;code&gt;linalg.matmul&lt;&#x2F;code&gt; is built for exactly that, and it&#x27;s the obvious target for lowering a matmul.&lt;&#x2F;p&gt;
&lt;p&gt;Block-scaled matmul breaks the uniformity on one side. In v1, only the &lt;code&gt;A&lt;&#x2F;code&gt; operand is quantized; &lt;code&gt;B&lt;&#x2F;code&gt; stays a dense &lt;code&gt;f32&lt;&#x2F;code&gt; tensor. The quantized operand &lt;code&gt;A&lt;&#x2F;code&gt; is not one tensor. It&#x27;s a mantissa tensor at full resolution and a scale tensor at one value per 32-element block along the contraction axis. For a &lt;code&gt;32×64&lt;&#x2F;code&gt; &lt;code&gt;A&lt;&#x2F;code&gt;, that is a &lt;code&gt;32×64&lt;&#x2F;code&gt; mantissa in &lt;code&gt;f8E4M3FN&lt;&#x2F;code&gt; and a &lt;code&gt;32×2&lt;&#x2F;code&gt; scale in &lt;code&gt;f8E8M0FNU&lt;&#x2F;code&gt;: the scale is 32 times coarser along &lt;code&gt;K&lt;&#x2F;code&gt; than the mantissa it belongs to. &lt;code&gt;A&lt;&#x2F;code&gt;&#x27;s true value at each position is &lt;code&gt;mantissa · scale_for_its_block&lt;&#x2F;code&gt;, and the two tensors are addressed on different strides.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;code&gt;linalg.matmul&lt;&#x2F;code&gt; cannot express that. It wants two uniform, identically-addressed operands. The only way to hand it a block-scaled &lt;code&gt;A&lt;&#x2F;code&gt; is to reconstruct the full-resolution values first: multiply every mantissa by its block&#x27;s scale into a dense &lt;code&gt;32×64&lt;&#x2F;code&gt; &lt;code&gt;f32&lt;&#x2F;code&gt; tensor, then multiply that by &lt;code&gt;B&lt;&#x2F;code&gt;. There is no ready-made op for it upstream either. The &lt;code&gt;linalg&lt;&#x2F;code&gt; structured ops are &lt;code&gt;generic&lt;&#x2F;code&gt;, &lt;code&gt;matmul&lt;&#x2F;code&gt;, &lt;code&gt;contract&lt;&#x2F;code&gt;, &lt;code&gt;batch_matmul&lt;&#x2F;code&gt;, and a handful of elementwise and reduction ops; none of them carries a block scale. The scale-aware primitives that do exist, &lt;code&gt;arith.scaling_extf&lt;&#x2F;code&gt; and &lt;code&gt;arith.scaling_truncf&lt;&#x2F;code&gt;, are elementwise conversions, not a contraction.&lt;&#x2F;p&gt;
&lt;p&gt;Reconstructing &lt;code&gt;A&lt;&#x2F;code&gt; is the one thing worth avoiding. The mantissa lives in &lt;code&gt;f8E4M3FN&lt;&#x2F;code&gt;, one byte per element. Expanding it to an &lt;code&gt;f32&lt;&#x2F;code&gt; reconstruction quadruples that to four bytes per element, and the resulting &lt;code&gt;32×64&lt;&#x2F;code&gt; tensor exists only to be read once by the multiply and thrown away. In a workload whose entire premise is cutting memory traffic, materializing a fat intermediate purely to feed a rigid op is the loss the lowering exists to prevent. Quantizing &lt;code&gt;A&lt;&#x2F;code&gt; and then immediately reconstructing it in full precision defeats the reason for quantizing it.&lt;&#x2F;p&gt;
&lt;p&gt;With the named ops ruled out, and reconstruction the only way to use one, the lowering target is &lt;code&gt;linalg.generic&lt;&#x2F;code&gt;, which lets each operand carry its own indexing map. That single choice, a generic with a custom map on the scale operand, is what the rest of this post is about. Four indexing maps, and one of them does all the work.&lt;&#x2F;p&gt;
&lt;h2 id=&quot;where-the-block-scaling-lives&quot;&gt;Where the block scaling lives&lt;a class=&quot;zola-anchor&quot; href=&quot;#where-the-block-scaling-lives&quot; aria-label=&quot;Anchor link for: where-the-block-scaling-lives&quot; style=&quot;visibility: hidden;&quot;&gt;&lt;&#x2F;a&gt;
&lt;&#x2F;h2&gt;
&lt;p&gt;The op going in is a single &lt;code&gt;mx.block_matmul&lt;&#x2F;code&gt; with three operands:&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;mlir&quot; class=&quot;language-mlir z-code&quot;&gt;&lt;code class=&quot;language-mlir&quot; data-lang=&quot;mlir&quot;&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;module {
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;  func.func @block_matmul(%arg0: !mx.tensor&amp;lt;32x64xf8E4M3FN, block_size = 32, scale_type = f8E8M0FNU&amp;gt;, %arg1: tensor&amp;lt;64x64xf32&amp;gt;, %arg2: tensor&amp;lt;32x64xf32&amp;gt;) -&amp;gt; tensor&amp;lt;32x64xf32&amp;gt; {
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;    %0 = mx.block_matmul %arg0, %arg1, %arg2 : &amp;lt;32x64xf8E4M3FN, block_size = 32, scale_type = f8E8M0FNU&amp;gt;, tensor&amp;lt;64x64xf32&amp;gt;, tensor&amp;lt;32x64xf32&amp;gt; -&amp;gt; tensor&amp;lt;32x64xf32&amp;gt;
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;    return %0 : tensor&amp;lt;32x64xf32&amp;gt;
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;  }
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;}
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;The block-scaled operand &lt;code&gt;%arg0&lt;&#x2F;code&gt; is one &lt;code&gt;!mx.tensor&lt;&#x2F;code&gt; value. The mantissa and its block scale are both inside that type; nothing about the split is visible yet. &lt;code&gt;%arg1&lt;&#x2F;code&gt; (&lt;code&gt;B&lt;&#x2F;code&gt;) and &lt;code&gt;%arg2&lt;&#x2F;code&gt; (the accumulator) are ordinary &lt;code&gt;f32&lt;&#x2F;code&gt; tensors. &lt;a href=&quot;https:&#x2F;&#x2F;josephbak.github.io&#x2F;posts&#x2F;designing-mx-dialect&#x2F;&quot;&gt;Post 1&lt;&#x2F;a&gt; covers how the type carries the block metadata.&lt;&#x2F;p&gt;
&lt;p&gt;Here is what &lt;code&gt;mx.block_matmul&lt;&#x2F;code&gt; lowers to:&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;mlir&quot; class=&quot;language-mlir z-code&quot;&gt;&lt;code class=&quot;language-mlir&quot; data-lang=&quot;mlir&quot;&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;#map = affine_map&amp;lt;(d0, d1, d2) -&amp;gt; (d0, d2)&amp;gt;
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;#map1 = affine_map&amp;lt;(d0, d1, d2) -&amp;gt; (d0, d2 floordiv 32)&amp;gt;
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;#map2 = affine_map&amp;lt;(d0, d1, d2) -&amp;gt; (d2, d1)&amp;gt;
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;#map3 = affine_map&amp;lt;(d0, d1, d2) -&amp;gt; (d0, d1)&amp;gt;
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;module {
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;  func.func @block_matmul(%arg0: tensor&amp;lt;32x64xf8E4M3FN&amp;gt;, %arg1: tensor&amp;lt;32x2xf8E8M0FNU&amp;gt;, %arg2: tensor&amp;lt;64x64xf32&amp;gt;, %arg3: tensor&amp;lt;32x64xf32&amp;gt;) -&amp;gt; tensor&amp;lt;32x64xf32&amp;gt; {
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;    %0 = linalg.generic {indexing_maps = [#map, #map1, #map2, #map3], iterator_types = [&amp;quot;parallel&amp;quot;, &amp;quot;parallel&amp;quot;, &amp;quot;reduction&amp;quot;]} ins(%arg0, %arg1, %arg2 : tensor&amp;lt;32x64xf8E4M3FN&amp;gt;, tensor&amp;lt;32x2xf8E8M0FNU&amp;gt;, tensor&amp;lt;64x64xf32&amp;gt;) outs(%arg3 : tensor&amp;lt;32x64xf32&amp;gt;) {
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;    ^bb0(%in: f8E4M3FN, %in_0: f8E8M0FNU, %in_1: f32, %out: f32):
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;      %1 = arith.extf %in : f8E4M3FN to f32
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;      %2 = arith.extf %in_0 : f8E8M0FNU to f32
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;      %3 = arith.mulf %1, %2 : f32
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;      %4 = arith.mulf %3, %in_1 : f32
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;      %5 = arith.addf %out, %4 : f32
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;      linalg.yield %5 : f32
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;    } -&amp;gt; tensor&amp;lt;32x64xf32&amp;gt;
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;    return %0 : tensor&amp;lt;32x64xf32&amp;gt;
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;  }
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;}
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;Set the payload region aside for the next section and look at the four indexing maps. The iteration space is &lt;code&gt;(d0, d1, d2)&lt;&#x2F;code&gt;, which is &lt;code&gt;(m, n, k)&lt;&#x2F;code&gt;: rows of the output, columns of the output, and the contraction axis. Three of the four maps are the ordinary matmul projections. &lt;code&gt;#map&lt;&#x2F;code&gt;, the mantissa, reads &lt;code&gt;(m, k)&lt;&#x2F;code&gt;. &lt;code&gt;#map2&lt;&#x2F;code&gt;, &lt;code&gt;B&lt;&#x2F;code&gt;, reads &lt;code&gt;(k, n)&lt;&#x2F;code&gt;. &lt;code&gt;#map3&lt;&#x2F;code&gt;, the accumulator, reads &lt;code&gt;(m, n)&lt;&#x2F;code&gt;. Each is a bare pair of iteration dimensions, passed straight through.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;code&gt;#map1&lt;&#x2F;code&gt;, the scale, is the one that isn&#x27;t ordinary:&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;mlir&quot; class=&quot;language-mlir z-code&quot;&gt;&lt;code class=&quot;language-mlir&quot; data-lang=&quot;mlir&quot;&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;#map1 = affine_map&amp;lt;(d0, d1, d2) -&amp;gt; (d0, d2 floordiv 32)&amp;gt;
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;As the contraction sweeps &lt;code&gt;k&lt;&#x2F;code&gt; from 0 to 63, the mantissa is read at every &lt;code&gt;k&lt;&#x2F;code&gt;, but the scale is read at &lt;code&gt;k floordiv 32&lt;&#x2F;code&gt;. For &lt;code&gt;k&lt;&#x2F;code&gt; in 0 through 31 that index is 0; at &lt;code&gt;k = 32&lt;&#x2F;code&gt; it becomes 1. One scale element is shared across a whole block of 32 mantissa elements, which is exactly what a block-scaled format means. The &lt;code&gt;32×2&lt;&#x2F;code&gt; shape of the scale operand is the same fact seen from the other side: 64 contraction positions divided into blocks of 32 gives 2 scale columns, one per block, &lt;code&gt;K &#x2F; 32&lt;&#x2F;code&gt;.&lt;&#x2F;p&gt;
&lt;p&gt;That single &lt;code&gt;floordiv&lt;&#x2F;code&gt; is the entire block-scaled semantics. Strip it out and this is a plain matmul; put it in and the scale operand is addressed at block resolution while everything else stays at element resolution. That asymmetry, the thing &lt;code&gt;linalg.matmul&lt;&#x2F;code&gt; could not express, is one arithmetic expression in one of four maps.&lt;&#x2F;p&gt;
&lt;p&gt;The maps say which element each iteration reads. The next section is what the iteration does with them.&lt;&#x2F;p&gt;
&lt;h2 id=&quot;the-payload-reconstructs-a-one-element-at-a-time&quot;&gt;The payload reconstructs &lt;code&gt;A&lt;&#x2F;code&gt; one element at a time&lt;a class=&quot;zola-anchor&quot; href=&quot;#the-payload-reconstructs-a-one-element-at-a-time&quot; aria-label=&quot;Anchor link for: the-payload-reconstructs-a-one-element-at-a-time&quot; style=&quot;visibility: hidden;&quot;&gt;&lt;&#x2F;a&gt;
&lt;&#x2F;h2&gt;
&lt;p&gt;Return to the &lt;code&gt;^bb0&lt;&#x2F;code&gt; region of that lowered op, on its own (the comments are added here; &lt;code&gt;mx-opt&lt;&#x2F;code&gt; prints the region without them):&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;mlir&quot; class=&quot;language-mlir z-code&quot;&gt;&lt;code class=&quot;language-mlir&quot; data-lang=&quot;mlir&quot;&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;^bb0(%in: f8E4M3FN, %in_0: f8E8M0FNU, %in_1: f32, %out: f32):
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;  %1 = arith.extf %in   : f8E4M3FN  to f32   &#x2F;&#x2F; mantissa -&amp;gt; f32
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;  %2 = arith.extf %in_0 : f8E8M0FNU to f32   &#x2F;&#x2F; scale    -&amp;gt; f32
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;  %3 = arith.mulf %1, %2 : f32               &#x2F;&#x2F; A_real = mantissa * scale
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;  %4 = arith.mulf %3, %in_1 : f32            &#x2F;&#x2F; A_real * B
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;  %5 = arith.addf %out, %4 : f32             &#x2F;&#x2F; accumulate into C
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;  linalg.yield %5 : f32
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;Four block arguments, one per operand, in the order the maps were listed: the mantissa scalar, the scale scalar, the &lt;code&gt;B&lt;&#x2F;code&gt; scalar, and the running accumulator. Each is a single value, already selected by that operand&#x27;s indexing map for this &lt;code&gt;(m, n, k)&lt;&#x2F;code&gt;. The payload never sees a tensor. It sees four numbers and produces one.&lt;&#x2F;p&gt;
&lt;p&gt;The work is a fused dequantize-then-multiply-accumulate. Widen the mantissa to &lt;code&gt;f32&lt;&#x2F;code&gt;, widen the scale to &lt;code&gt;f32&lt;&#x2F;code&gt;, multiply them to reconstruct &lt;code&gt;A&lt;&#x2F;code&gt;&#x27;s true value at this position, multiply that by &lt;code&gt;B&lt;&#x2F;code&gt;, add to the accumulator. The reconstruction &lt;code&gt;mantissa * scale&lt;&#x2F;code&gt; and the contraction &lt;code&gt;* B&lt;&#x2F;code&gt; happen in the same region, on the same scalar, in registers. &lt;code&gt;A&lt;&#x2F;code&gt;&#x27;s full-precision value exists for the duration of one multiply-accumulate and is gone.&lt;&#x2F;p&gt;
&lt;p&gt;That is the point of lowering to a fused generic rather than a dequantize op followed by a matmul. Dequantize-then-matmul would compute every &lt;code&gt;A_real&lt;&#x2F;code&gt;, write all of them into a dense &lt;code&gt;32×64&lt;&#x2F;code&gt; &lt;code&gt;f32&lt;&#x2F;code&gt; tensor, and then read that tensor back in the matmul. The fused form computes each &lt;code&gt;A_real&lt;&#x2F;code&gt; exactly where it is consumed and never writes it anywhere. The reconstructed &lt;code&gt;A&lt;&#x2F;code&gt; is never a value in the program: &lt;code&gt;%3&lt;&#x2F;code&gt; is a scalar in a register, produced and consumed inside one loop iteration, not a tensor-typed result that has to live in memory. There is no &lt;code&gt;32×64&lt;&#x2F;code&gt; &lt;code&gt;f32&lt;&#x2F;code&gt; intermediate to write and reread. Keeping &lt;code&gt;A&lt;&#x2F;code&gt; in its one-byte form instead of expanding it to a four-byte reconstruction is the memory-traffic win the dialect exists for.&lt;&#x2F;p&gt;
&lt;p&gt;The grouping is deliberate. Floating-point multiply is not associative: each product rounds to the nearest representable value, and regrouping changes which intermediate gets rounded. The payload computes &lt;code&gt;(mantissa * scale) * B&lt;&#x2F;code&gt;, not &lt;code&gt;(mantissa * B) * scale&lt;&#x2F;code&gt;. Because the E8M0 scale is a power of two, &lt;code&gt;mantissa * scale&lt;&#x2F;code&gt; is exact whenever the result stays in the format&#x27;s normal range: multiplying by a power of two only shifts the exponent and leaves the mantissa bits untouched, so nothing rounds. (At the extremes, a product that overflows to infinity or underflows into the subnormal range can still lose precision; the v1 lowering assumes in-range blocks.) Reconstructing &lt;code&gt;A&lt;&#x2F;code&gt;&#x27;s true value first and doing the one lossy multiply against &lt;code&gt;B&lt;&#x2F;code&gt; last is faithful to what the op means: dequantize &lt;code&gt;A&lt;&#x2F;code&gt;, then contract. The other grouping would round a meaningless &lt;code&gt;mantissa * B&lt;&#x2F;code&gt; intermediate and then scale the rounding error.&lt;&#x2F;p&gt;
&lt;p&gt;One thing the IR does not do: exploit that the scale is a power of two. At the IR level &lt;code&gt;arith.mulf %1, %2&lt;&#x2F;code&gt; is a general &lt;code&gt;f32&lt;&#x2F;code&gt; multiply, and nothing records that &lt;code&gt;%2&lt;&#x2F;code&gt; is constrained to powers of two. A backend emits an ordinary floating-point multiply, not the exponent-add the power-of-two case would in principle allow. The exactness is a property to reason about for correctness, not an optimization the v1 lowering expresses.&lt;&#x2F;p&gt;
&lt;h2 id=&quot;one-floordiv-two-bars&quot;&gt;One floordiv, two bars&lt;a class=&quot;zola-anchor&quot; href=&quot;#one-floordiv-two-bars&quot; aria-label=&quot;Anchor link for: one-floordiv-two-bars&quot; style=&quot;visibility: hidden;&quot;&gt;&lt;&#x2F;a&gt;
&lt;&#x2F;h2&gt;
&lt;p&gt;&lt;code&gt;#map1&lt;&#x2F;code&gt;, the scale map, puts a &lt;code&gt;floordiv&lt;&#x2F;code&gt; inside an indexing map: &lt;code&gt;(d0, d2 floordiv 32)&lt;&#x2F;code&gt;. The &lt;code&gt;linalg.generic&lt;&#x2F;code&gt; verifies with it; a named matmul op would not.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;code&gt;linalg.matmul&lt;&#x2F;code&gt; and &lt;code&gt;linalg.contract&lt;&#x2F;code&gt; require every indexing map to be a projected permutation: each result is a bare iteration dimension, passed through with no arithmetic. &lt;code&gt;d2 floordiv 32&lt;&#x2F;code&gt; is arithmetic on a dimension, so it is not a projected permutation, and a named op&#x27;s verifier refuses it outright with &quot;provided affine_map is not a projected permutation.&quot;&lt;&#x2F;p&gt;
&lt;p&gt;&lt;code&gt;linalg.generic&lt;&#x2F;code&gt; does not impose that requirement. Its verifier asks a weaker, structural question: taken together, do the operands&#x27; indexing maps let you recover the iteration space? Concatenate all four maps into one map from loops to operand coordinates, then try to invert it. If the concatenated map inverts, the op is well-formed; if it doesn&#x27;t, the op is rejected. The check is on the whole system, not on each map in isolation.&lt;&#x2F;p&gt;
&lt;p&gt;The scale map, on its own, does not invert: we cannot recover &lt;code&gt;k&lt;&#x2F;code&gt; from &lt;code&gt;k floordiv 32&lt;&#x2F;code&gt;, since 32 different values of &lt;code&gt;k&lt;&#x2F;code&gt; collapse to the same block index. But it does not have to invert on its own. The mantissa map &lt;code&gt;(m, k)&lt;&#x2F;code&gt; exposes both &lt;code&gt;m&lt;&#x2F;code&gt; and &lt;code&gt;k&lt;&#x2F;code&gt; directly, and the &lt;code&gt;B&lt;&#x2F;code&gt; map &lt;code&gt;(k, n)&lt;&#x2F;code&gt; adds &lt;code&gt;n&lt;&#x2F;code&gt;. All three of &lt;code&gt;m&lt;&#x2F;code&gt;, &lt;code&gt;n&lt;&#x2F;code&gt;, and &lt;code&gt;k&lt;&#x2F;code&gt; are recoverable from operands other than the scale, so the scale map rides along on coordinates the system has already pinned down. The concatenated system inverts, and the generic accepts it.&lt;&#x2F;p&gt;
&lt;p&gt;Whether a non-projective map is fatal depends entirely on that whole-system question. The &lt;code&gt;quantize&lt;&#x2F;code&gt; op in this same dialect shows the other outcome. Its per-block reduction wants to address a block index &lt;code&gt;b&lt;&#x2F;code&gt; and a within-block index &lt;code&gt;j&lt;&#x2F;code&gt; at once, and written naively its input map reads &lt;code&gt;(i, b, j) -&amp;gt; (i, b * 32 + j)&lt;&#x2F;code&gt;, two iteration dimensions fused into one coordinate. There the fusion is fatal, because nothing recovers them: no other operand exposes &lt;code&gt;b&lt;&#x2F;code&gt; and &lt;code&gt;j&lt;&#x2F;code&gt; separately, so &lt;code&gt;b * 32 + j = 32&lt;&#x2F;code&gt; could be &lt;code&gt;(b=1, j=0)&lt;&#x2F;code&gt; or &lt;code&gt;(b=0, j=32)&lt;&#x2F;code&gt; and the system cannot tell which. The map does not invert, the op is rejected, and &lt;code&gt;quantize&lt;&#x2F;code&gt; has to reshape its input with &lt;code&gt;tensor.expand_shape&lt;&#x2F;code&gt; so &lt;code&gt;b&lt;&#x2F;code&gt; and &lt;code&gt;j&lt;&#x2F;code&gt; land on separate axes first. The matmul&#x27;s &lt;code&gt;floordiv&lt;&#x2F;code&gt; never hits this. It transforms one dimension that another operand already pins down, so there is nothing left to recover.&lt;&#x2F;p&gt;
&lt;p&gt;This is why the lowering targets a generic in the first place. The op set was designed to be expressible as &lt;code&gt;linalg.generic&lt;&#x2F;code&gt;, not abstracted away from it, and the block-scale access is exactly the kind of map a generic permits and a named matmul op does not. The escape hatch is deliberate: &lt;code&gt;linalg.generic&lt;&#x2F;code&gt; trades the per-map guarantee for a whole-system one, and the block scale needs precisely that trade.&lt;&#x2F;p&gt;
&lt;h2 id=&quot;the-second-bar-comes-later&quot;&gt;The second bar comes later&lt;a class=&quot;zola-anchor&quot; href=&quot;#the-second-bar-comes-later&quot; aria-label=&quot;Anchor link for: the-second-bar-comes-later&quot; style=&quot;visibility: hidden;&quot;&gt;&lt;&#x2F;a&gt;
&lt;&#x2F;h2&gt;
&lt;p&gt;The generic cleared the &lt;code&gt;floordiv&lt;&#x2F;code&gt;, but it did not make it a projected permutation. It only postponed the moment that matters.&lt;&#x2F;p&gt;
&lt;p&gt;The per-map bar that a named matmul op enforces at definition does not disappear when we lower to a generic instead. It reappears in the structured vectorizer. To vectorize a &lt;code&gt;linalg.generic&lt;&#x2F;code&gt;, every indexing map must be a projected permutation, the same requirement &lt;code&gt;linalg.matmul&lt;&#x2F;code&gt; would have imposed up front. The vectorizer will look at &lt;code&gt;d2 floordiv 32&lt;&#x2F;code&gt;, see arithmetic on a dimension, and refuse it, exactly as the named op would have.&lt;&#x2F;p&gt;
&lt;p&gt;Nothing about the map changed between lowering and vectorization. What changed is which check it faces. The generic waived the per-map bar and asked only that the whole system invert, so the &lt;code&gt;floordiv&lt;&#x2F;code&gt; passed. The vectorizer does not waive it. The same access that read cleanly one pass ago is the one it cannot take as written.&lt;&#x2F;p&gt;
&lt;p&gt;So the choice that made the lowering clean is the choice that blocks vectorization. One &lt;code&gt;floordiv&lt;&#x2F;code&gt;, legal in the op it lowered to and rejected by the transform that has to run on it next. That collision, and what it takes past it, is a problem for later in the series.&lt;&#x2F;p&gt;
&lt;h2 id=&quot;the-map-and-what-follows&quot;&gt;The map and what follows&lt;a class=&quot;zola-anchor&quot; href=&quot;#the-map-and-what-follows&quot; aria-label=&quot;Anchor link for: the-map-and-what-follows&quot; style=&quot;visibility: hidden;&quot;&gt;&lt;&#x2F;a&gt;
&lt;&#x2F;h2&gt;
&lt;p&gt;Post 4 takes a different thread down the pipeline: how one &lt;code&gt;!mx.tensor&lt;&#x2F;code&gt; value becomes two memory buffers, and how the accumulator this op carries turns into an in-place write. Post 5 returns to the &lt;code&gt;floordiv&lt;&#x2F;code&gt; and drives the dialect to running code, where the vectorizer&#x27;s refusal has to be answered.&lt;&#x2F;p&gt;
&lt;p&gt;The thesis was that block-scaled matmul reduces to one &lt;code&gt;floordiv&lt;&#x2F;code&gt; in one affine map. It is the whole block structure, it is why the op lowers to a &lt;code&gt;linalg.generic&lt;&#x2F;code&gt;, and it is the reason the clean lowering carries a cost into the next pass. One choice in one map, right for the stage it was built for and pointed straight at the next problem. The map is the design; everything else in the lowering follows it.&lt;&#x2F;p&gt;
</content>
	</entry>
	<entry xml:lang="en">
		<title>Canonicalization and the E8M0 Power-of-Two Trick</title>
		<published>2026-08-17T00:00:00+00:00</published>
		<updated>2026-08-17T00:00:00+00:00</updated>
		<link rel="alternate" type="text/html" href="https://josephbak.github.io/posts/canonicalization-e8m0-power-of-two/"/>
		<id>https://josephbak.github.io/posts/canonicalization-e8m0-power-of-two/</id>
    
		<content type="html" xml:base="https://josephbak.github.io/posts/canonicalization-e8m0-power-of-two/">&lt;!-- KaTeX includes (inline; requires markdown.render_unsafe = true) --&gt;
&lt;link rel=&quot;stylesheet&quot; href=&quot;https:&#x2F;&#x2F;cdn.jsdelivr.net&#x2F;npm&#x2F;katex@0.16.10&#x2F;dist&#x2F;katex.min.css&quot;&gt;
&lt;script defer src=&quot;https:&#x2F;&#x2F;cdn.jsdelivr.net&#x2F;npm&#x2F;katex@0.16.10&#x2F;dist&#x2F;katex.min.js&quot;&gt;&lt;&#x2F;script&gt;
&lt;script defer src=&quot;https:&#x2F;&#x2F;cdn.jsdelivr.net&#x2F;npm&#x2F;katex@0.16.10&#x2F;dist&#x2F;contrib&#x2F;auto-render.min.js&quot;&gt;&lt;&#x2F;script&gt;
&lt;script&gt;
 document.addEventListener(&quot;DOMContentLoaded&quot;, function() {
 renderMathInElement(document.body, {
 delimiters: [
 {left: &quot;$$&quot;, right: &quot;$$&quot;, display: true},
 {left: &quot;\\[&quot;, right: &quot;\\]&quot;, display: true},
 {left: &quot;$&quot;, right: &quot;$&quot;, display: false},
 {left: &quot;\\(&quot;, right: &quot;\\)&quot;, display: false}
 ],
 throwOnError: false
 });
 });
&lt;&#x2F;script&gt;
&lt;!-- SECTION 1 — the op named after a trick it doesn&#x27;t perform --&gt;
&lt;h2 id=&quot;the-fold-in-the-name&quot;&gt;The fold in the name&lt;a class=&quot;zola-anchor&quot; href=&quot;#the-fold-in-the-name&quot; aria-label=&quot;Anchor link for: the-fold-in-the-name&quot; style=&quot;visibility: hidden;&quot;&gt;&lt;&#x2F;a&gt;
&lt;&#x2F;h2&gt;
&lt;p&gt;The dialect has an operation called &lt;code&gt;mx.fold_scale&lt;&#x2F;code&gt;. The name describes a specific, elegant rewrite: a scale-only multiply sitting in front of a block-scaled value can be folded &lt;em&gt;into&lt;&#x2F;em&gt; the shared scale instead of touching the data. For a block-scaled tensor, the value model is $X_i = S \cdot M_i$: one shared scale $S$ per block, one mantissa $M_i$ per element (&lt;a href=&quot;https:&#x2F;&#x2F;josephbak.github.io&#x2F;posts&#x2F;designing-mx-dialect&#x2F;&quot;&gt;Post 1&lt;&#x2F;a&gt; covers the type and the four ops). Multiplying the whole block by a scalar $\alpha$ is then just scalar associativity:&lt;&#x2F;p&gt;
&lt;p&gt;$$\alpha \cdot (S \cdot M_i) = (\alpha \cdot S) \cdot M_i$$&lt;&#x2F;p&gt;
&lt;p&gt;Push $\alpha$ into $S$ and we rescale an entire block by rewriting one scale value,
leaving every mantissa untouched. That is what &quot;fold scale&quot; means.&lt;&#x2F;p&gt;
&lt;p&gt;v1 never does it.&lt;&#x2F;p&gt;
&lt;p&gt;The scale $S$ is stored as &lt;code&gt;f8E8M0FNU&lt;&#x2F;code&gt;: an 8-bit exponent, zero mantissa bits, representing powers of two and nothing else (Post 1 leans on this; it&#x27;s the whole reason the format is cheap). That structure is what would make the fold exact: if $\alpha$ is itself a power of two, then $\alpha \cdot S$ is a power of two too, and folding becomes a single integer add on the exponent field. No rounding, no new storage, type-safe. It is one of the cleanest rewrites the format allows, and v1 ships three canonicalization patterns, none of which perform it.&lt;&#x2F;p&gt;
&lt;p&gt;This post is about that gap. The exponent-field add is the intuition the op is named for, not the code v1 runs. What v1 runs is three rewrites that &lt;em&gt;clear&lt;&#x2F;em&gt; &lt;code&gt;fold_scale&lt;&#x2F;code&gt; out of the IR before it reaches lowering, by merging constants, dropping identities, and relocating the scale into the f32 domain, never once writing an E8M0 scale. Why v1 clears the op instead of lowering it is the point. First, what makes the fold exact at all?&lt;&#x2F;p&gt;
&lt;!-- SECTION 2 — the exponent-field add: the intuition you&#x27;re about to not use --&gt;
&lt;h2 id=&quot;why-powers-of-two-make-it-exact&quot;&gt;Why powers of two make it exact&lt;a class=&quot;zola-anchor&quot; href=&quot;#why-powers-of-two-make-it-exact&quot; aria-label=&quot;Anchor link for: why-powers-of-two-make-it-exact&quot; style=&quot;visibility: hidden;&quot;&gt;&lt;&#x2F;a&gt;
&lt;&#x2F;h2&gt;
&lt;p&gt;Start with what &lt;code&gt;f8E8M0FNU&lt;&#x2F;code&gt; represents: $2^e$ for the stored exponent $e$, nothing else. There is no fraction to carry, no mantissa to round. The representable set is exactly the powers of two, and the encoding is just the integer $e$.&lt;&#x2F;p&gt;
&lt;p&gt;Now multiply two of them. A scale $S = 2^e$, a scalar $\alpha$ that also happens to be a power of two, $\alpha = 2^a$:&lt;&#x2F;p&gt;
&lt;p&gt;$$\alpha \cdot S = 2^{a} \cdot 2^{e} = 2^{a+e}$$&lt;&#x2F;p&gt;
&lt;p&gt;The product is a power of two, so it lands exactly on a representable E8M0 value, as long as $a+e$ stays within the exponent&#x27;s range. The add is always exact; the encoding can still underflow or overflow at the extremes.&lt;&#x2F;p&gt;
&lt;p&gt;Computing it doesn&#x27;t require a floating-point multiply at all: $a + e$ is just an integer add on the exponent field. No rounding step exists in that path, so there is nothing to lose. That is the exponent-field add, and it is why folding a power-of-two scalar into an E8M0 scale is exact by construction.&lt;&#x2F;p&gt;
&lt;p&gt;The alternative shows what &quot;exact&quot; is buying us. Take $\alpha = 4.5$. Now:&lt;&#x2F;p&gt;
&lt;p&gt;$$\alpha \cdot S = 4.5 \cdot 2^{e}$$&lt;&#x2F;p&gt;
&lt;p&gt;That is not a power of two. It sits between $2^{e+2} = 4 \cdot 2^{e}$ and $2^{e+3} = 8 \cdot 2^{e}$, and &lt;code&gt;f8E8M0FNU&lt;&#x2F;code&gt; has no encoding for anything in between. To store it we would have to round to one neighbor or the other, and neither is the value we wanted. So folding a non-power-of-two scalar into the scale is not the same exact operation applied to a different number. It is a different operation: one that silently corrupts the scale, or fails the verifier if we check. &quot;Exact&quot; means the result stays inside E8M0&#x27;s representable set with no rounding; only a power-of-two $\alpha$ does that.&lt;&#x2F;p&gt;
&lt;p&gt;This is the trick the operation is named for. It is genuinely elegant: a scale
multiply reduced to an integer add, exact, touching one scale value instead of a
whole block. Everything in the setup points at implementing it.&lt;&#x2F;p&gt;
&lt;!-- SECTION 3 — why v1 doesn&#x27;t absorb into S: the causal chain --&gt;
&lt;h2 id=&quot;why-v1-doesn-t-do-it&quot;&gt;Why v1 doesn&#x27;t do it&lt;a class=&quot;zola-anchor&quot; href=&quot;#why-v1-doesn-t-do-it&quot; aria-label=&quot;Anchor link for: why-v1-doesn-t-do-it&quot; style=&quot;visibility: hidden;&quot;&gt;&lt;&#x2F;a&gt;
&lt;&#x2F;h2&gt;
&lt;p&gt;The reason isn&#x27;t that the exponent-field add is hard. It&#x27;s one integer add. The reason is what implementing it would drag in.&lt;&#x2F;p&gt;
&lt;p&gt;Folding $\alpha$ into $S$ means writing a &lt;em&gt;new scale value&lt;&#x2F;em&gt;. The add $a + e$ produces an exponent that has to be stored back into the scale tensor. That is not a rewrite of the IR&#x27;s structure: it&#x27;s a computation that produces data and puts it somewhere. Producing-and-storing a value is what lowering does, not what canonicalization does. Canonicalization rewrites IR into simpler IR at the same level; it never emits the runtime arithmetic that computes a scale and writes it
down. So the elegant one-line trick, taken seriously, is a lowering.&lt;&#x2F;p&gt;
&lt;p&gt;That reclassification is the whole cost. A &lt;code&gt;fold_scale&lt;&#x2F;code&gt; with a real lowering path is an op that survives canonicalization and arrives at the conversion stage needing a conversion pattern, a bufferized result, and — the part that matters — a guard. Because once $\alpha$ reaches an E8M0 scale, a non-power-of-two $\alpha$ is a live correctness bug. $4.5 \cdot S$ has to round to a wrong power of two, silently. The lowering can&#x27;t be shipped without a check that $\alpha$ is a power of two, rejecting or specially handling everything else. The guard isn&#x27;t optional decoration on the lowering; it&#x27;s the lowering&#x27;s admission that the trick only works on the values it was designed for.&lt;&#x2F;p&gt;
&lt;p&gt;v1&#x27;s thesis doesn&#x27;t need any of that. The point of v1 is the dialect design and the canonicalization contract: that &lt;code&gt;fold_scale&lt;&#x2F;code&gt; is well-formed, that it composes, and that it can be cleared from the IR before lowering. A runtime scale-rewrite proves none of that better than clearing the op does, and it costs a whole
lowering path plus a mandatory guard. So v1 makes the opposite call. &lt;code&gt;fold_scale&lt;&#x2F;code&gt; stays canonicalization-only: no lowering, no guard, no scale ever rewritten. The op is not lowered: it is &lt;em&gt;eliminated&lt;&#x2F;em&gt;, before the conversion stage ever sees it.&lt;&#x2F;p&gt;
&lt;p&gt;That single decision is what the three patterns exist to carry out. If &lt;code&gt;fold_scale&lt;&#x2F;code&gt; will never be lowered, then every &lt;code&gt;fold_scale&lt;&#x2F;code&gt; in the IR has to be gone by the time conversion runs, because conversion has no pattern for it and a surviving op is a hard error. Clearing it is not an optimization. It is the thing that makes the canonicalization-only decision safe.&lt;&#x2F;p&gt;
&lt;h2 id=&quot;three-patterns-three-shapes&quot;&gt;Three patterns, three shapes&lt;a class=&quot;zola-anchor&quot; href=&quot;#three-patterns-three-shapes&quot; aria-label=&quot;Anchor link for: three-patterns-three-shapes&quot; style=&quot;visibility: hidden;&quot;&gt;&lt;&#x2F;a&gt;
&lt;&#x2F;h2&gt;
&lt;p&gt;Clearing every &lt;code&gt;fold_scale&lt;&#x2F;code&gt; before conversion is the job of three canonicalization patterns. None touches $S$; each matches a different shape of IR and clears it a different way.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;Merge — &lt;code&gt;FoldScalePow2&lt;&#x2F;code&gt;.&lt;&#x2F;strong&gt; Two &lt;code&gt;fold_scale&lt;&#x2F;code&gt; ops stacked on the same value
collapse into one. Given&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;mlir&quot; class=&quot;language-mlir z-code&quot;&gt;&lt;code class=&quot;language-mlir&quot; data-lang=&quot;mlir&quot;&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;%inner = mx.fold_scale %t, %alpha1 : !mx.tensor&amp;lt;...&amp;gt;, f32 -&amp;gt; !mx.tensor&amp;lt;...&amp;gt;
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;%outer = mx.fold_scale %inner, %alpha2 : !mx.tensor&amp;lt;...&amp;gt;, f32 -&amp;gt; !mx.tensor&amp;lt;...&amp;gt;
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;with &lt;code&gt;%alpha1 = 4.0&lt;&#x2F;code&gt; and &lt;code&gt;%alpha2 = 8.0&lt;&#x2F;code&gt;, the pattern replaces both with a single &lt;code&gt;fold_scale&lt;&#x2F;code&gt; carrying the product:&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;mlir&quot; class=&quot;language-mlir z-code&quot;&gt;&lt;code class=&quot;language-mlir&quot; data-lang=&quot;mlir&quot;&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;%c = arith.constant 3.200000e+01 : f32   &#x2F;&#x2F; 4.0 \cdot 8.0, folded at compile time
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;%r = mx.fold_scale %t, %c : !mx.tensor&amp;lt;...&amp;gt;, f32 -&amp;gt; !mx.tensor&amp;lt;...&amp;gt;
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;It fires only when both scalars are compile-time constants, because it computes
the product right there with an &lt;code&gt;APFloat&lt;&#x2F;code&gt; multiply and bakes the result in. The
greedy driver reapplies it, so a three-deep stack ($2.0$, $4.0$, $8.0$) collapses
to one op holding $64.0$. Two honest notes. There is no power-of-two check on the
constants despite the name: it folds $4.0 \cdot 8.0$ the same as it would fold any
two floats, because the product is never written into an E8M0 scale, so a
non-power-of-two result harms nothing. The fold is an f32 &lt;code&gt;APFloat&lt;&#x2F;code&gt; multiply,
not the exponent-field add from the intuition. Same result for constant inputs,
different mechanism: the exponent-field add is &lt;em&gt;why folding would be exact&lt;&#x2F;em&gt;; the
&lt;code&gt;APFloat&lt;&#x2F;code&gt; multiply is &lt;em&gt;what the code does&lt;&#x2F;em&gt;.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;Eliminate — &lt;code&gt;FoldScaleIdentity&lt;&#x2F;code&gt;.&lt;&#x2F;strong&gt; Scaling by one is a no-op. The pattern checks
the scalar is exactly $1.0$ and rewires every use of the result straight to the
input:&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;mlir&quot; class=&quot;language-mlir z-code&quot;&gt;&lt;code class=&quot;language-mlir&quot; data-lang=&quot;mlir&quot;&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;%r = mx.fold_scale %t, %one : ...   &#x2F;&#x2F; %one = 1.0
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;&#x2F;&#x2F; disappears; uses of %r become %t
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;No new op. This is elimination, not folding.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;Relocate — &lt;code&gt;DequantizeFoldScaleFusion&lt;&#x2F;code&gt;.&lt;&#x2F;strong&gt; A cross-op rewrite, and the only one
that clears a &lt;code&gt;fold_scale&lt;&#x2F;code&gt; by moving its scalar somewhere else. Given a
&lt;code&gt;dequantize_block&lt;&#x2F;code&gt; fed by a &lt;code&gt;fold_scale&lt;&#x2F;code&gt;:&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;mlir&quot; class=&quot;language-mlir z-code&quot;&gt;&lt;code class=&quot;language-mlir&quot; data-lang=&quot;mlir&quot;&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;%s = mx.fold_scale %t, %alpha : !mx.tensor&amp;lt;...&amp;gt;, f32 -&amp;gt; !mx.tensor&amp;lt;...&amp;gt;
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;%d = mx.dequantize_block %s : !mx.tensor&amp;lt;...&amp;gt; -&amp;gt; tensor&amp;lt;32x64xf32&amp;gt;
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;it dequantizes the original tensor first, then applies $\alpha$ as an ordinary f32
multiply on the result:&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;mlir&quot; class=&quot;language-mlir z-code&quot;&gt;&lt;code class=&quot;language-mlir&quot; data-lang=&quot;mlir&quot;&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;%d = mx.dequantize_block %t : !mx.tensor&amp;lt;...&amp;gt; -&amp;gt; tensor&amp;lt;32x64xf32&amp;gt;
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;%sp = tensor.splat %alpha : tensor&amp;lt;32x64xf32&amp;gt;
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;%r  = arith.mulf %d, %sp : tensor&amp;lt;32x64xf32&amp;gt;
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;The pattern matches on the &lt;em&gt;consumer&lt;&#x2F;em&gt; (&lt;code&gt;dequantize_block&lt;&#x2F;code&gt;) and walks backward to its producer with &lt;code&gt;getDefiningOp&amp;lt;FoldScaleOp&amp;gt;()&lt;&#x2F;code&gt;. If the producer is a &lt;code&gt;fold_scale&lt;&#x2F;code&gt;, it rewrites the pair. The &lt;code&gt;fold_scale&lt;&#x2F;code&gt; is gone; the scaling survives as a plain float multiply on the dequantized tensor. Fusion never evaluates $\alpha$, it just hands it to a runtime &lt;code&gt;arith.mulf&lt;&#x2F;code&gt;, so a runtime $\alpha$ works as well as a constant: nothing here asks whether $\alpha$ is a power of two, or even whether it&#x27;s known at compile time. When $\alpha$ &lt;em&gt;is&lt;&#x2F;em&gt; a constant, the greedy driver folds the splat into a &lt;code&gt;dense&lt;&#x2F;code&gt; attribute (&lt;code&gt;arith.constant dense&amp;lt;4.000000e+00&amp;gt; : tensor&amp;lt;32x64xf32&amp;gt;&lt;&#x2F;code&gt;); when it&#x27;s a function argument, the &lt;code&gt;tensor.splat %arg&lt;&#x2F;code&gt; stays. Same pattern, two shapes of output.&lt;&#x2F;p&gt;
&lt;p&gt;Where each pattern is registered matters. &lt;code&gt;FoldScalePow2&lt;&#x2F;code&gt; and &lt;code&gt;FoldScaleIdentity&lt;&#x2F;code&gt; hang off &lt;code&gt;FoldScaleOp&lt;&#x2F;code&gt;: they match a &lt;code&gt;fold_scale&lt;&#x2F;code&gt; directly. &lt;code&gt;DequantizeFoldScaleFusion&lt;&#x2F;code&gt; hangs off &lt;code&gt;DeQuantizeBlockOp&lt;&#x2F;code&gt;, not &lt;code&gt;FoldScaleOp&lt;&#x2F;code&gt;. It matches the dequantize consumer and walks back to the &lt;code&gt;fold_scale&lt;&#x2F;code&gt; it removes. A pattern is registered on the op it matches, not the op it deletes.&lt;&#x2F;p&gt;
&lt;p&gt;Three shapes: a stack, an identity, a dequantize-consumer. Three clearings: merge, eliminate, relocate. All three are &lt;code&gt;OpRewritePattern&lt;&#x2F;code&gt;, greedy-driver canonicalizations, opportunistic and level-preserving. Not one writes a scale.&lt;&#x2F;p&gt;
&lt;h2 id=&quot;what-the-patterns-don-t-cover&quot;&gt;What the patterns don&#x27;t cover&lt;a class=&quot;zola-anchor&quot; href=&quot;#what-the-patterns-don-t-cover&quot; aria-label=&quot;Anchor link for: what-the-patterns-don-t-cover&quot; style=&quot;visibility: hidden;&quot;&gt;&lt;&#x2F;a&gt;
&lt;&#x2F;h2&gt;
&lt;p&gt;The three patterns cover three shapes, and their union has holes.&lt;&#x2F;p&gt;
&lt;p&gt;Take a single &lt;code&gt;fold_scale&lt;&#x2F;code&gt; whose result feeds something other than a &lt;code&gt;dequantize_block&lt;&#x2F;code&gt;, such as a function return or a &lt;code&gt;block_matmul&lt;&#x2F;code&gt;:&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;mlir&quot; class=&quot;language-mlir z-code&quot;&gt;&lt;code class=&quot;language-mlir&quot; data-lang=&quot;mlir&quot;&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;%s = mx.fold_scale %t, %c : !mx.tensor&amp;lt;...&amp;gt;, f32 -&amp;gt; !mx.tensor&amp;lt;...&amp;gt;
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;return %s : !mx.tensor&amp;lt;...&amp;gt;
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;No pattern fires. &lt;code&gt;FoldScalePow2&lt;&#x2F;code&gt; needs a &lt;em&gt;stack&lt;&#x2F;em&gt; of two &lt;code&gt;fold_scale&lt;&#x2F;code&gt; ops; there&#x27;s
only one. &lt;code&gt;FoldScaleIdentity&lt;&#x2F;code&gt; needs $\alpha$ to be exactly $1.0$; this $\alpha$ isn&#x27;t.
&lt;code&gt;DequantizeFoldScaleFusion&lt;&#x2F;code&gt; needs the consumer to be a &lt;code&gt;dequantize_block&lt;&#x2F;code&gt;; a
&lt;code&gt;return&lt;&#x2F;code&gt; isn&#x27;t. So the op survives canonicalization untouched, and here&#x27;s the part that matters: whether &lt;code&gt;%c&lt;&#x2F;code&gt; is a clean power of two like $2.0$ is irrelevant. No pattern even inspects it, because no pattern absorbs $\alpha$ into a scale. The power-of-two question, the whole exactness story from the first half of this post, never comes up.&lt;&#x2F;p&gt;
&lt;p&gt;What happens to the survivor is the point. It flows into the conversion pass, which
marks the &lt;code&gt;mx&lt;&#x2F;code&gt; dialect illegal and must translate every &lt;code&gt;mx&lt;&#x2F;code&gt; op to &lt;code&gt;linalg&lt;&#x2F;code&gt;.
&lt;code&gt;fold_scale&lt;&#x2F;code&gt; has no conversion pattern, by design; it was only ever meant to be
canonicalized away. So the conversion driver hits an illegal op it cannot rewrite
and fails the whole compilation:&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;mlir&quot; class=&quot;language-mlir z-code&quot;&gt;&lt;code class=&quot;language-mlir&quot; data-lang=&quot;mlir&quot;&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;error: failed to legalize operation &amp;#39;mx.fold_scale&amp;#39; that was explicitly marked illegal
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;That is not a bug. It&#x27;s the boundary v1 drew, enforcing itself. The two drivers behave oppositely on an unmatched op: canonicalization is opportunistic, so no match is a no-op; conversion is mandatory, so no match is a hard error. A &lt;code&gt;fold_scale&lt;&#x2F;code&gt; the three patterns don&#x27;t clear sails through the first driver and dies in the second. The failure is loud and it happens at compile time, before any wrong number is produced. Compare the alternative: a lowering that quietly rounds $4.5 \cdot S$ to the wrong power of two and ships a silently corrupt scale. Between a clear compile-time failure on an unsupported input and a silent runtime corruption, v1 takes the failure.&lt;&#x2F;p&gt;
&lt;p&gt;Making the coverage total is possible, but it takes a real &lt;code&gt;fold_scale&lt;&#x2F;code&gt; lowering: one that applies $\alpha$ without leaning on a downstream &lt;code&gt;dequantize_block&lt;&#x2F;code&gt; to absorb it. That&#x27;s a cleaner design on the coverage axis, and more than v1 needs. v1&#x27;s job is the dialect and the canonicalization contract, and for the shapes v1 supports, the three patterns clear every &lt;code&gt;fold_scale&lt;&#x2F;code&gt; before conversion.&lt;&#x2F;p&gt;
&lt;h2 id=&quot;what-it-would-take-to-make-it-exact&quot;&gt;What it would take to make it exact&lt;a class=&quot;zola-anchor&quot; href=&quot;#what-it-would-take-to-make-it-exact&quot; aria-label=&quot;Anchor link for: what-it-would-take-to-make-it-exact&quot; style=&quot;visibility: hidden;&quot;&gt;&lt;&#x2F;a&gt;
&lt;&#x2F;h2&gt;
&lt;p&gt;The real lowering is where the power-of-two restriction has to be enforced. The exponent-field add from the top of this post, the exact one-integer-add fold the op is named for, is what it would run.&lt;&#x2F;p&gt;
&lt;p&gt;Writing a new scale means computing $\alpha \cdot S$ and storing it into the &lt;code&gt;f8E8M0FNU&lt;&#x2F;code&gt; scale tensor. In v1, $\alpha$ is harmless no matter what it is: &lt;code&gt;FoldScalePow2&lt;&#x2F;code&gt; multiplies $4.5$ into another f32 constant and leaves it as an f32 value on the op. f32 holds $4.5$ exactly, and the value never reaches a scale. The moment a lowering tries to write $\alpha \cdot S$ into the scale, that changes. If $\alpha = 4.5$, then $\alpha \cdot S$ is not a power of two, and &lt;code&gt;f8E8M0FNU&lt;&#x2F;code&gt; cannot store it: a verifier failure, or a silent round to the wrong power of two, the exact corruption v1 avoided by never writing the scale at all.&lt;&#x2F;p&gt;
&lt;p&gt;So the guard &lt;code&gt;FoldScalePow2&lt;&#x2F;code&gt; conspicuously lacks becomes mandatory the instant absorption is real. The lowering checks that $\alpha$ is a power of two, takes the exponent-field add when it is, and widens to a larger scale type when it isn&#x27;t: keep the E8M0 block scale and carry the non-power-of-two part as a separate f32 factor, so the value reconstructs as two scales multiplied instead of one. That check isn&#x27;t defensive boilerplate. It&#x27;s the lowering admitting what the E8M0 format demanded all along: the fold is exact only on the values the scale type can represent, and the exponent-field add is what &quot;exact&quot; looks like when it can.&lt;&#x2F;p&gt;
&lt;p&gt;v1 keeps &lt;code&gt;fold_scale&lt;&#x2F;code&gt; on the canonicalization side of the line, where $\alpha$ never touches a scale and the power-of-two question never has to be asked. v1.5 moves it across, at which point the question is unavoidable and the guard is the answer. The op has carried the name of the exact fold the whole time. v1 just never had to make it exact.&lt;&#x2F;p&gt;
</content>
	</entry>
	<entry xml:lang="en">
		<title>A Dialect Is a Set of Co-Designed Decisions: Designing for MX Quantization in MLIR</title>
		<published>2026-08-12T00:00:00+00:00</published>
		<updated>2026-08-12T00:00:00+00:00</updated>
		<link rel="alternate" type="text/html" href="https://josephbak.github.io/posts/designing-mx-dialect/"/>
		<id>https://josephbak.github.io/posts/designing-mx-dialect/</id>
    
		<content type="html" xml:base="https://josephbak.github.io/posts/designing-mx-dialect/">&lt;!-- KaTeX includes (inline; requires markdown.render_unsafe = true) --&gt;
&lt;link rel=&quot;stylesheet&quot; href=&quot;https:&#x2F;&#x2F;cdn.jsdelivr.net&#x2F;npm&#x2F;katex@0.16.10&#x2F;dist&#x2F;katex.min.css&quot;&gt;
&lt;script defer src=&quot;https:&#x2F;&#x2F;cdn.jsdelivr.net&#x2F;npm&#x2F;katex@0.16.10&#x2F;dist&#x2F;katex.min.js&quot;&gt;&lt;&#x2F;script&gt;
&lt;script defer src=&quot;https:&#x2F;&#x2F;cdn.jsdelivr.net&#x2F;npm&#x2F;katex@0.16.10&#x2F;dist&#x2F;contrib&#x2F;auto-render.min.js&quot;&gt;&lt;&#x2F;script&gt;
&lt;script&gt;
 document.addEventListener(&quot;DOMContentLoaded&quot;, function() {
 renderMathInElement(document.body, {
 delimiters: [
 {left: &quot;$$&quot;, right: &quot;$$&quot;, display: true},
 {left: &quot;\\[&quot;, right: &quot;\\]&quot;, display: true},
 {left: &quot;$&quot;, right: &quot;$&quot;, display: false},
 {left: &quot;\\(&quot;, right: &quot;\\)&quot;, display: false}
 ],
 throwOnError: false
 });
 });
&lt;&#x2F;script&gt;
&lt;p&gt;Modern hardware can do arithmetic far faster than it can fetch the numbers to compute on. For large-model inference the bottleneck is rarely the multiply-accumulate units. It&#x27;s memory bandwidth, the cost of moving weights and activations from DRAM to the compute. Quantization attacks that bottleneck directly: store each number in fewer bits, move fewer bytes, and spend the machine&#x27;s idle arithmetic capacity reconstructing full-precision values on the fly. It trades FLOPs, which are cheap, for memory traffic, which is not.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;code&gt;mlir-mx&lt;&#x2F;code&gt; is an out-of-tree MLIR dialect implementing OCP Microscaling (MX) block-scaled quantization, the format behind NVIDIA Blackwell and AMD&#x27;s MI300-series accelerators. The design goal is clear: represent block-scaled data in the IR, and lower it to code that moves those smaller bytes and reconstructs full precision on the fly. Four operations cover the core: &lt;code&gt;quantize_block&lt;&#x2F;code&gt; (pack f32 into a mantissa plus a shared scale), &lt;code&gt;dequantize_block&lt;&#x2F;code&gt; (the inverse), &lt;code&gt;block_matmul&lt;&#x2F;code&gt; (multiply while still block-scaled), and &lt;code&gt;fold_scale&lt;&#x2F;code&gt; (a canonicalization for scale-only multiplies, the subject of Post 2). Plus one parameterized type, &lt;code&gt;!mx.tensor&lt;&#x2F;code&gt;, carrying the block-scale metadata.&lt;&#x2F;p&gt;
&lt;p&gt;That list, a type and four ops, makes it look like a dialect is a parts catalog: pick a type, pick some operations, done. It isn&#x27;t. &lt;strong&gt;A dialect is a set of co-designed decisions, and the type, ops, and attributes are just where those decisions become visible.&lt;&#x2F;strong&gt; A dialect gets built in stages: first you define the types, then the ops, then the lowerings that turn those ops into simpler IR, then bufferization that turns values into memory. These stages are not independent. The type&#x27;s parameters are shaped by what the ops need; the ops&#x27; signatures by how they&#x27;ll lower; the lowering by how it&#x27;ll schedule and bufferize. A choice made at the type stage can be forced by something that doesn&#x27;t happen until the lowering stage, several passes later. Pull one decision and others move. The rest of this post walks the type and the four ops. Two forces get the full treatment: the &lt;strong&gt;block axis&lt;&#x2F;strong&gt; of the type, forced by how the matmul reduces, and the &lt;strong&gt;accumulator operand&lt;&#x2F;strong&gt; of &lt;code&gt;block_matmul&lt;&#x2F;code&gt;, forced by how it bufferizes.&lt;&#x2F;p&gt;
&lt;h2 id=&quot;what-block-scaling-is&quot;&gt;What block scaling is&lt;a class=&quot;zola-anchor&quot; href=&quot;#what-block-scaling-is&quot; aria-label=&quot;Anchor link for: what-block-scaling-is&quot; style=&quot;visibility: hidden;&quot;&gt;&lt;&#x2F;a&gt;
&lt;&#x2F;h2&gt;
&lt;p&gt;Ordinary quantization picks one scale factor for a whole tensor: every value is stored as a small integer or low-bit float, times that one shared number. The problem is outliers: one large value forces a coarse scale, and all the small values lose precision under it.&lt;&#x2F;p&gt;
&lt;p&gt;Block scaling fixes this by using many scales instead of one. Split the data into fixed-size blocks of 32 contiguous elements, in MX, and give each block its own scale. A value is reconstructed as &lt;code&gt;mantissa × block_scale&lt;&#x2F;code&gt;:&lt;&#x2F;p&gt;
&lt;pre class=&quot;z-code&quot;&gt;&lt;code&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;values:  [ v0  v1  v2  ...  v31 ][ v32  v33  ...  v63 ]
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;         └──────── block 0 ──────┘└─────── block 1 ────┘
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;scale:            S0                        S1
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;                  (one shared scale per 32 elements)
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;Each element stores a small mantissa; each block of 32 stores one shared scale. OCP MX (Open Compute Project Microscaling) is the cross-vendor standard for this: block size 32, an FP8 mantissa, and a power-of-two scale. It&#x27;s what NVIDIA Blackwell and AMD&#x27;s MI300-series accelerators run in hardware.&lt;&#x2F;p&gt;
&lt;h2 id=&quot;the-type-mx-tensor&quot;&gt;The type: &lt;code&gt;!mx.tensor&lt;&#x2F;code&gt;&lt;a class=&quot;zola-anchor&quot; href=&quot;#the-type-mx-tensor&quot; aria-label=&quot;Anchor link for: the-type-mx-tensor&quot; style=&quot;visibility: hidden;&quot;&gt;&lt;&#x2F;a&gt;
&lt;&#x2F;h2&gt;
&lt;p&gt;Every op in the dialect operates on one type, so it gets defined first. An &lt;code&gt;!mx.tensor&lt;&#x2F;code&gt; is a block-scaled tensor: low-bit mantissa data plus the metadata needed to interpret it. Written out:&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;mlir&quot; class=&quot;language-mlir z-code&quot;&gt;&lt;code class=&quot;language-mlir&quot; data-lang=&quot;mlir&quot;&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;!mx.tensor&amp;lt;32x64xf8E4M3FN, block_size = 32, scale_type = f8E8M0FNU&amp;gt;
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;&lt;code&gt;MX_TensorType&lt;&#x2F;code&gt; is a custom type defined from scratch in the &lt;code&gt;mx&lt;&#x2F;code&gt; dialect (a TableGen &lt;code&gt;TypeDef&lt;&#x2F;code&gt;, mnemonic &lt;code&gt;tensor&lt;&#x2F;code&gt;, so it prints as &lt;code&gt;!mx.tensor&lt;&#x2F;code&gt;). It is &lt;strong&gt;not&lt;&#x2F;strong&gt; a builtin &lt;code&gt;RankedTensorType&lt;&#x2F;code&gt; and does not inherit from one. It carries four parameters: a &lt;code&gt;shape&lt;&#x2F;code&gt; (an array of dimension sizes, the same &lt;code&gt;int64_t&lt;&#x2F;code&gt;-array representation MLIR&#x27;s own shaped types use), an &lt;code&gt;elementType&lt;&#x2F;code&gt; for the mantissa, a &lt;code&gt;blockSize&lt;&#x2F;code&gt;, and a &lt;code&gt;scaleType&lt;&#x2F;code&gt;. The first two mirror what a ranked tensor holds; the last two are what make it &lt;em&gt;block-scaled&lt;&#x2F;em&gt; rather than merely low-bit.&lt;&#x2F;p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;shape&lt;&#x2F;code&gt;, &lt;code&gt;elementType&lt;&#x2F;code&gt;&lt;&#x2F;strong&gt; (&lt;code&gt;32x64&lt;&#x2F;code&gt;, &lt;code&gt;f8E4M3FN&lt;&#x2F;code&gt;): the mantissa tensor. &lt;code&gt;f8E4M3FN&lt;&#x2F;code&gt; is an 8-bit float, 4 exponent bits and 3 mantissa bits, holding each quantized value.&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;block_size&lt;&#x2F;code&gt;&lt;&#x2F;strong&gt; (&lt;code&gt;32&lt;&#x2F;code&gt;): how many contiguous elements share one scale. 32 is the OCP MX block size.&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;scale_type&lt;&#x2F;code&gt;&lt;&#x2F;strong&gt; (&lt;code&gt;f8E8M0FNU&lt;&#x2F;code&gt;): the shared scale&#x27;s element type. E8M0 is 8 exponent bits and zero mantissa bits, so it represents powers of two and nothing else. Why that constraint is useful is Post 2&#x27;s subject.&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
&lt;p&gt;Making it an independent type, rather than reusing &lt;code&gt;RankedTensorType&lt;&#x2F;code&gt; or implementing MLIR&#x27;s &lt;code&gt;ShapedTypeInterface&lt;&#x2F;code&gt;, is a deliberate choice. A block-scaled tensor is not a single shaped value: at bufferization it decomposes into a mantissa tensor and a separate scale tensor. Implementing the shaped-type interface would promise the 1:1 tensor semantics that decomposition deliberately breaks. That story is Post 4; the point here is that even the &lt;em&gt;type&lt;&#x2F;em&gt; was defined with the lowering in mind.&lt;&#x2F;p&gt;
&lt;p&gt;Two flags on the definition matter later. &lt;code&gt;hasCustomAssemblyFormat = 1&lt;&#x2F;code&gt; means the &lt;code&gt;NxNx...&lt;&#x2F;code&gt; syntax is parsed by hand rather than by a declarative format; the mechanics of that (and the rule to use &lt;code&gt;getChecked&lt;&#x2F;code&gt; over &lt;code&gt;get&lt;&#x2F;code&gt; in a parser) are in a linked note. &lt;code&gt;genVerifyDecl = 1&lt;&#x2F;code&gt; means the type has a verifier: it checks that &lt;code&gt;blockSize &amp;gt; 0&lt;&#x2F;code&gt; and that the block size evenly divides the last dimension. Structural checks only — no scale math, no value semantics. The verifier&#x27;s entire job is well-formedness.&lt;&#x2F;p&gt;
&lt;p&gt;The type stores the scale&#x27;s &lt;em&gt;type&lt;&#x2F;em&gt; and &lt;em&gt;how many elements it covers&lt;&#x2F;em&gt;, never the scale values themselves. A helper, &lt;code&gt;getNumScaleBlocks()&lt;&#x2F;code&gt;, computes how many scales a tensor needs (one per block along the last axis), but that&#x27;s a count, not the data. This is just what a type is in MLIR: a compile-time label, not runtime storage. Where the scale values actually live is a bufferization question with a non-obvious answer, and it gets its own post (Post 4).&lt;&#x2F;p&gt;
&lt;p&gt;One parameter is doing more work than it looks. The type says block size 32, but not &lt;em&gt;which axis&lt;&#x2F;em&gt; is blocked. So it fixes a convention: &lt;strong&gt;blocking always runs along the last axis.&lt;&#x2F;strong&gt; That looks like an arbitrary tidiness rule. It isn&#x27;t. It&#x27;s the first of the two co-designed decisions, and the thing that forces it doesn&#x27;t appear until the matmul lowers, well into the pipeline. We&#x27;ll come back to it once the ops are on the table.&lt;&#x2F;p&gt;
&lt;h2 id=&quot;the-four-ops&quot;&gt;The four ops&lt;a class=&quot;zola-anchor&quot; href=&quot;#the-four-ops&quot; aria-label=&quot;Anchor link for: the-four-ops&quot; style=&quot;visibility: hidden;&quot;&gt;&lt;&#x2F;a&gt;
&lt;&#x2F;h2&gt;
&lt;p&gt;A note on scope: this is v1, a deliberately minimal first version with four ops and one type, and some features held back to later versions (marked v1.5 or v2 as they come up).&lt;&#x2F;p&gt;
&lt;p&gt;With the type defined, the four operations follow. Each takes or produces an &lt;code&gt;!mx.tensor&lt;&#x2F;code&gt; and does one job.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;&lt;code&gt;quantize_block&lt;&#x2F;code&gt;&lt;&#x2F;strong&gt; turns a normal &lt;code&gt;f32&lt;&#x2F;code&gt; tensor into a block-scaled one. It reads a &lt;code&gt;tensor&amp;lt;...xf32&amp;gt;&lt;&#x2F;code&gt; and produces an &lt;code&gt;!mx.tensor&lt;&#x2F;code&gt;: for each block of 32 elements it picks a shared scale, then rounds each element against that scale into the low-bit mantissa type. This is the lossy step. The whole round-trip&#x27;s precision loss happens here, when 32 full-precision values are forced to share one power-of-two scale and each is rounded to three mantissa bits.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;&lt;code&gt;dequantize_block&lt;&#x2F;code&gt;&lt;&#x2F;strong&gt; is the inverse: &lt;code&gt;!mx.tensor&lt;&#x2F;code&gt; back to &lt;code&gt;tensor&amp;lt;...xf32&amp;gt;&lt;&#x2F;code&gt;. For each element it multiplies the mantissa by its block&#x27;s scale. Because the scale is a power of two, that multiply just shifts the exponent and adds no error of its own, so dequantize faithfully reconstructs whatever the quantize step produced, no more and no less.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;&lt;code&gt;block_matmul&lt;&#x2F;code&gt;&lt;&#x2F;strong&gt; is the point of the whole dialect: multiply while the data is still block-scaled, so the smaller bytes are what moves. It takes a block-scaled &lt;code&gt;A&lt;&#x2F;code&gt; of shape &lt;code&gt;M×K&lt;&#x2F;code&gt;, an ordinary &lt;code&gt;f32&lt;&#x2F;code&gt; matrix &lt;code&gt;B&lt;&#x2F;code&gt; of shape &lt;code&gt;K×N&lt;&#x2F;code&gt;, and an accumulator &lt;code&gt;C&lt;&#x2F;code&gt; of shape &lt;code&gt;M×N&lt;&#x2F;code&gt;, and computes &lt;code&gt;C += A · B&lt;&#x2F;code&gt;. The reduction runs along &lt;code&gt;K&lt;&#x2F;code&gt;, the shared dimension the two matrices multiply over. As the sum sweeps &lt;code&gt;K&lt;&#x2F;code&gt;, &lt;code&gt;A&lt;&#x2F;code&gt;&#x27;s mantissas are read per element while &lt;code&gt;A&lt;&#x2F;code&gt;&#x27;s scales are read once per block of 32. The scale indexing is coarser than the mantissa indexing by exactly the block size. That coarseness is what settles the deferred question of which axis gets blocked.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;&lt;code&gt;fold_scale&lt;&#x2F;code&gt;&lt;&#x2F;strong&gt; doesn&#x27;t quantize, dequantize, or multiply. It&#x27;s a canonicalization, an algebraic cleanup the compiler applies before code generation, for the case where a block-scaled value gets multiplied by a scalar. Rather than touch every element, the scalar can be handled more cheaply by rewriting the surrounding ops. It carries no lowering of its own and must be gone before the conversion pass runs. (Post 2 covers which forms v1 folds and the power-of-two property in play.)&lt;&#x2F;p&gt;
&lt;p&gt;One detail of &lt;code&gt;block_matmul&lt;&#x2F;code&gt;&#x27;s signature will matter later: it carries the accumulator &lt;code&gt;C&lt;&#x2F;code&gt; as an operand, not just a result. That&#x27;s the second co-designed decision, and like the first, its justification shows up further down the pipeline.&lt;&#x2F;p&gt;
&lt;h2 id=&quot;the-block-axis&quot;&gt;The block axis&lt;a class=&quot;zola-anchor&quot; href=&quot;#the-block-axis&quot; aria-label=&quot;Anchor link for: the-block-axis&quot; style=&quot;visibility: hidden;&quot;&gt;&lt;&#x2F;a&gt;
&lt;&#x2F;h2&gt;
&lt;p&gt;Back to the deferred question: the type blocks along the last axis. Why fix that as a rule?&lt;&#x2F;p&gt;
&lt;p&gt;Start from what &lt;code&gt;block_matmul&lt;&#x2F;code&gt; computes. Each output element is a sum over &lt;code&gt;K&lt;&#x2F;code&gt;:&lt;&#x2F;p&gt;
&lt;pre class=&quot;z-code&quot;&gt;&lt;code&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;C[m,n] = Σ  A[m,k] · B[k,n]
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;     k=0..K-1
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;&lt;code&gt;A&lt;&#x2F;code&gt; is block-scaled, so &lt;code&gt;A[m,k]&lt;&#x2F;code&gt; isn&#x27;t stored directly. It&#x27;s a mantissa times its block&#x27;s scale. The whole question is: &lt;strong&gt;what does that scale depend on?&lt;&#x2F;strong&gt; And that depends entirely on which axis is blocked.&lt;&#x2F;p&gt;
&lt;p&gt;Block along &lt;code&gt;K&lt;&#x2F;code&gt;, the last axis, which is what the type fixes. Then the scale for &lt;code&gt;A[m,k]&lt;&#x2F;code&gt; is &lt;code&gt;S[m, k&#x2F;32]&lt;&#x2F;code&gt;: one scale per row, shared across each run of 32 consecutive &lt;code&gt;k&lt;&#x2F;code&gt;. The sharing runs along the reduction.&lt;&#x2F;p&gt;
&lt;pre class=&quot;z-code&quot;&gt;&lt;code&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;row m of A, K = 64, two blocks along k:
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;  k:  0  1  2 ...  31     32  33 ...  63
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;      └── S[m,0] ──┘      └── S[m,1] ──┘
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;      one scale for       one scale for
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;      this run of k       this run of k
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;  reduction sweeps k  ──────────────▶
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;  scale is constant within each run it sums over
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;Because the scale is constant across each block of the sum, it factors out of that block&#x27;s partial sum: 32 mantissa terms share one scale. The scale aligns with the direction being summed.&lt;&#x2F;p&gt;
&lt;p&gt;Now block along &lt;code&gt;M&lt;&#x2F;code&gt;, the rows, instead. The scale for &lt;code&gt;A[m,k]&lt;&#x2F;code&gt; becomes &lt;code&gt;S[m&#x2F;32, k]&lt;&#x2F;code&gt;: shared across 32 rows, but varying with &lt;code&gt;k&lt;&#x2F;code&gt;.&lt;&#x2F;p&gt;
&lt;pre class=&quot;z-code&quot;&gt;&lt;code&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;reduction sweeps k  ──────────────▶
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;  k =    0       1       2     ...
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;       S[·,0]  S[·,1]  S[·,2]  ...
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;       └─ a different scale at every k ─┘
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;
&lt;&#x2F;span&gt;&lt;span class=&quot;z-text z-plain&quot;&gt;  scale changes on every step of the sum it&amp;#39;s inside
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;Now the scale sits inside the reduction and changes on every step. It can&#x27;t factor out; every term of the sum carries its own scale. The block sharing (across rows) is orthogonal to the reduction (along &lt;code&gt;k&lt;&#x2F;code&gt;), so the one structural advantage of block scaling, a shared scale amortized over a run, is unavailable to the matmul.&lt;&#x2F;p&gt;
&lt;p&gt;Here&#x27;s the part worth being precise about, because it&#x27;s easy to overstate. Blocking along &lt;code&gt;M&lt;&#x2F;code&gt; doesn&#x27;t cost more storage. The same mantissas and the same number of scales get stored; the count is &lt;code&gt;M·K&#x2F;32&lt;&#x2F;code&gt; either way, the same ~4× compression over &lt;code&gt;f32&lt;&#x2F;code&gt;. What&#x27;s lost is exploitability. The scale can&#x27;t align with the reduction, so the matmul can&#x27;t use the block structure: the format keeps its storage cost and loses its computational benefit. That is what &quot;no traffic win&quot; means. The bytes don&#x27;t grow; the one operation the dialect exists for just can&#x27;t take advantage of them.&lt;&#x2F;p&gt;
&lt;p&gt;So the last-axis rule isn&#x27;t tidiness. It&#x27;s forced: &lt;strong&gt;the block axis must be the reduction axis, because a shared scale only aligns with a matmul if it&#x27;s constant along the axis being summed.&lt;&#x2F;strong&gt; For &lt;code&gt;A&lt;&#x2F;code&gt; of shape &lt;code&gt;M×K&lt;&#x2F;code&gt;, the reduction axis is &lt;code&gt;K&lt;&#x2F;code&gt;, which is the last axis. The type fixes last-axis blocking, and &lt;code&gt;block_matmul&lt;&#x2F;code&gt;&#x27;s reduction is the reason.&lt;&#x2F;p&gt;
&lt;p&gt;That&#x27;s the first cross-stage force, stated plainly: a choice at the type stage (which axis carries the blocks), justified only by a fact about the lowering stage (how the matmul reduces), which runs passes later. Define the type without the matmul in mind and last-axis looks arbitrary; you might just as easily block the rows. Design the two stages together and it&#x27;s the only choice that works.&lt;&#x2F;p&gt;
&lt;p&gt;One honest edge, since it shows where the rule&#x27;s generality stops. &quot;Last axis&quot; is really shorthand for &quot;the reduction axis,&quot; and those coincide only because &lt;code&gt;A&lt;&#x2F;code&gt; is the left operand, whose reduction axis is last. A quantized &lt;code&gt;B&lt;&#x2F;code&gt;, deferred to v1.5, reduces along its first axis, so the same principle would put its blocks there. The convention is last-axis for &lt;code&gt;A&lt;&#x2F;code&gt;; the law underneath is reduction-axis. When &lt;code&gt;B&lt;&#x2F;code&gt; gets quantized, the type will need an explicit block-axis parameter rather than a fixed last-axis rule.&lt;&#x2F;p&gt;
&lt;p&gt;The block axis was a choice at the type stage, forced by the lowering stage. The next decision is the same shape, one stage further down: a choice in an op&#x27;s signature, forced by bufferization.&lt;&#x2F;p&gt;
&lt;h2 id=&quot;the-accumulator-operand&quot;&gt;The accumulator operand&lt;a class=&quot;zola-anchor&quot; href=&quot;#the-accumulator-operand&quot; aria-label=&quot;Anchor link for: the-accumulator-operand&quot; style=&quot;visibility: hidden;&quot;&gt;&lt;&#x2F;a&gt;
&lt;&#x2F;h2&gt;
&lt;p&gt;This is the second of the two forces. The first was a type-stage choice forced by the lowering stage. This one is an op-signature choice forced by bufferization, near the end of the pipeline.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;code&gt;block_matmul&lt;&#x2F;code&gt; takes three operands: the block-scaled &lt;code&gt;A&lt;&#x2F;code&gt;, the matrix &lt;code&gt;B&lt;&#x2F;code&gt;, and an accumulator &lt;code&gt;C&lt;&#x2F;code&gt;. It computes &lt;code&gt;C += A · B&lt;&#x2F;code&gt;. That third operand is the one to look at. &lt;code&gt;C&lt;&#x2F;code&gt; is both an input and the result: the op reads the incoming &lt;code&gt;C&lt;&#x2F;code&gt;, adds &lt;code&gt;A · B&lt;&#x2F;code&gt; to it, and produces the updated &lt;code&gt;C&lt;&#x2F;code&gt;. It could have been written the other way, as a pure &lt;code&gt;C = A · B&lt;&#x2F;code&gt; with &lt;code&gt;C&lt;&#x2F;code&gt; as a result only, no accumulator input. Why carry &lt;code&gt;C&lt;&#x2F;code&gt; in?&lt;&#x2F;p&gt;
&lt;p&gt;The answer doesn&#x27;t live in the op definition or the lowering. It lives at bufferization, where tensors become memory.&lt;&#x2F;p&gt;
&lt;p&gt;A quick frame for what bufferization does. Up through the lowering, everything is &lt;em&gt;value-semantic&lt;&#x2F;em&gt;: a tensor is a mathematical value, not a location in memory, and every op conceptually produces a fresh one. That&#x27;s clean to reason about but not how hardware works, since hardware writes to buffers. Bufferization is the stage that assigns each tensor value a concrete memory buffer, and its central question for every op is whether the op can write its result into an existing buffer, in place, or needs a fresh allocation and a copy.&lt;&#x2F;p&gt;
&lt;p&gt;In-place is what you want. A matmul that allocates a new &lt;code&gt;M×N&lt;&#x2F;code&gt; buffer on every call and then copies is moving bytes the algorithm doesn&#x27;t require, which is exactly the memory traffic this dialect exists to cut. For the benchmark to measure only essential traffic, the accumulator has to be updated in place.&lt;&#x2F;p&gt;
&lt;p&gt;That is what the &lt;code&gt;C&lt;&#x2F;code&gt; operand makes possible. Because &lt;code&gt;C&lt;&#x2F;code&gt; is passed in as an operand, the op&#x27;s result can reuse &lt;code&gt;C&lt;&#x2F;code&gt;&#x27;s own buffer: the GEMM (General Matrix Multiply) form &lt;code&gt;C += A · B&lt;&#x2F;code&gt; names a destination, &quot;write the result back into &lt;code&gt;C&lt;&#x2F;code&gt;.&quot; This is destination-passing style (DPS), the pattern MLIR&#x27;s own &lt;code&gt;linalg&lt;&#x2F;code&gt; ops use for the same reason. Write the op the other way, &lt;code&gt;C = A · B&lt;&#x2F;code&gt; with no accumulator input, and there&#x27;s no incoming buffer for the result to reuse at all.&lt;&#x2F;p&gt;
&lt;p&gt;The distinction is exactly the right size to be careful about. The accumulator operand doesn&#x27;t &lt;em&gt;guarantee&lt;&#x2F;em&gt; in-place accumulation; it &lt;em&gt;enables&lt;&#x2F;em&gt; it. Whether the write actually happens in place is decided later, by bufferization&#x27;s own conflict analysis and its function-boundary handling, which is Post 4&#x27;s subject. What the operand settles, at definition time, is whether in-place is available at all. The pure &lt;code&gt;C = A · B&lt;&#x2F;code&gt; form forecloses it: no incoming buffer, nothing for any later analysis to reuse, a fresh allocation guaranteed. The GEMM form keeps the option open for bufferization to take.&lt;&#x2F;p&gt;
&lt;p&gt;So the accumulator operand is not a modeling nicety. It&#x27;s the op signature being shaped, at definition time, to preserve an option that only a much later stage can act on. That&#x27;s the same force as the block axis, one stage over: an earlier choice made to serve a need further down the pipeline.&lt;&#x2F;p&gt;
&lt;p&gt;Two decisions, the same shape. The block axis was the type stage constrained by the lowering stage. The accumulator was the op stage constrained by the bufferization stage. Neither makes sense from inside its own stage: last-axis blocking looks arbitrary next to the type, the accumulator operand looks like a stylistic quirk next to the op. Each is justified only by a stage further down. That&#x27;s what &quot;co-designed&quot; means in practice, and it&#x27;s why a dialect is not a parts catalog. The parts are easy. The constraints between them, running across stages from type definition to the end of the pipeline, are the actual design.&lt;&#x2F;p&gt;
&lt;h2 id=&quot;what-s-deferred-and-what-s-next&quot;&gt;What&#x27;s deferred, and what&#x27;s next&lt;a class=&quot;zola-anchor&quot; href=&quot;#what-s-deferred-and-what-s-next&quot; aria-label=&quot;Anchor link for: what-s-deferred-and-what-s-next&quot; style=&quot;visibility: hidden;&quot;&gt;&lt;&#x2F;a&gt;
&lt;&#x2F;h2&gt;
&lt;p&gt;v1 is four ops and one type, and the boundaries are deliberate. The things left out are the ones that would have grown the scope past what one version should carry: a quantized &lt;code&gt;B&lt;&#x2F;code&gt; operand (v1 leaves &lt;code&gt;B&lt;&#x2F;code&gt; in &lt;code&gt;f32&lt;&#x2F;code&gt;), per-channel scales, group quantization across dimension boundaries, and a real lowering for &lt;code&gt;fold_scale&lt;&#x2F;code&gt; (v1 only cleans it up, never lowers it). Further out, past v1.5 and v2, the natural direction is lowering these ops to accelerator backends where MX is a native hardware format rather than something emulated on CPU. That&#x27;s the long arc; v1 is the IR foundation it would stand on.&lt;&#x2F;p&gt;
&lt;p&gt;The rest of the series follows the pipeline down, roughly one stage per post:&lt;&#x2F;p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Post 2&lt;&#x2F;strong&gt; takes &lt;code&gt;fold_scale&lt;&#x2F;code&gt; and the power-of-two scale type, and shows why folding a block scale is exact for some scalars and impossible for others.&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;Post 3&lt;&#x2F;strong&gt; is the lowering: &lt;code&gt;block_matmul&lt;&#x2F;code&gt; becomes a &lt;code&gt;linalg.generic&lt;&#x2F;code&gt;, and the block scale becomes a single &lt;code&gt;floordiv&lt;&#x2F;code&gt; in an affine map. It&#x27;s the technical center of the project.&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;Post 4&lt;&#x2F;strong&gt; is bufferization: how one &lt;code&gt;!mx.tensor&lt;&#x2F;code&gt; value becomes two memory buffers, and how the in-place accumulation promised above actually gets decided.&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;Post 5&lt;&#x2F;strong&gt; drives the whole thing to running code, runs into two upstream limits in MLIR, and quantifies the memory traffic the dialect was built to cut.&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
&lt;p&gt;The thesis was that a dialect is a set of co-designed decisions, not a parts catalog. The two moments in this post were the evidence: a type that blocks its last axis because a matmul later in the pipeline reduces along it, and an op that carries an accumulator because bufferization near the end of the pipeline needs a destination to write into. The ops are the easy part. What makes it a dialect is that the decisions behind them are wired to each other, across the whole pipeline.&lt;&#x2F;p&gt;
</content>
	</entry>
</feed>
